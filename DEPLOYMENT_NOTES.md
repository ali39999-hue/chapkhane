# یادداشت‌های استقرار (Deployment Notes)

> [!WARNING]
> این پروژه به صورت محلی (Local) روی سرویس‌های بومی ویندوز (Native Services) تست شده است. دلیل این امر محدودیت‌های مربوط به `nested virtualization` در ماشین مجازی (VM) فعلی است که از اجرای صحیح `WSL2` و `Docker Desktop` جلوگیری می‌کند.

## نکته حیاتی قبل از لانچ نهایی (Production Launch)
پیش از دیپلوی نهایی و زیر بار بردن سیستم، **حتماً** باید کل پروژه یک بار به صورت کامل روی یک سرور لینوکسی واقعی (VPS) با استفاده از دستور زیر تست شود:

```bash
docker compose up --build -d
```

تا اطمینان حاصل شود که معماری کانتینری (شامل `redis`، `minio` و `preflight-worker`) بین بخش‌های مختلف سیستم بدون هیچ نقصی کار می‌کند. کدهای نوشته شده کاملاً منطبق با داکر هستند و با تغییر فایل `.env` روی سرور به راحتی به محیط پروداکشن منتقل خواهند شد.
